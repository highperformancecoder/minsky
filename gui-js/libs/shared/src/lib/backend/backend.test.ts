import {describe, expect, test} from '@jest/globals';
import {minsky} from './minsky'

describe('properties',()=>{
  test('get-set',async ()=>{
    let m=await minsky.properties();
//    minsky.properties().then((m)=>{
//      expect("t" in m).toBe(true);
//      m.t+=1;
//      minsky.properties(m).
//        then((p)=>{
//          minsky.properties().then((p)=>{
//            expect(p.t).toBe(m.t);
//          });
//        });
//    });
  });
});

         
